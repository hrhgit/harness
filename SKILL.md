---
name: harness
description: 自动识别用户对当前项目表达的长期偏好或工程规范，将具体反馈适度抽象为可复用规则，并写入项目根目录 AGENTS.md 的 `## Harness`。只要语义上约束未来同类工作，无论用户是否明确要求保存，都应使用；一次性要求继续按当前任务处理。发现新旧规范冲突时，暂停工作并请用户确认生效规则。
---

# Harness

把用户明确表达的项目级长期约束写入项目自己的 `AGENTS.md`，让后续智能体在新的会话和任务中继续遵循。记录应简短、可执行，并完整保留文件中的其他已有指令。

## 自动识别长期意图

每次用户给出偏好、纠正、约束、默认选择或实现原则时，主动从完整语义判断它是否在定义未来同类工作的做法。用户明确要求保存和用户自然表达长期规则具有同等效力。

从四个维度理解语义：

1. **范围**：表达指向整个项目、一个功能类别、公共组件、统一数据边界或反复出现的工作。
2. **持续性**：表达意在后续继续生效，或具有开放的有效期。长期意图可以由语义体现，也可以包含明确时间词。
3. **规范性**：用户在规定应该、必须、优先或默认怎么做。
4. **复用性**：该规则能指导未来至少一个不同文件、页面、模块或同类功能。

按以下三档处理：

- **明确长期**：四个维度清晰，或用户明确要求记住/固化。直接写入 Harness。
- **可能长期**：像偏好或规范，但适用范围、期限或项目归属不清。用一句问题确认是否要作为项目长期规范。
- **明显临时**：只服务当前交付、实验、迁移阶段或单个对象。将它应用于当前任务即可。

强信号包括但不限于：

- 范围概括：“所有表单”“这类接口”“公共组件”“项目里的 ID”。
- 规范语气：“统一”“默认”“必须”“优先”“一律”“这类都这样”。
- 持续信号：“以后”“始终”“接下来都”“从现在开始”“保持这个规则”。
- 纠正模式：“这种地方统一用 Lucide”“类似迁移都要保留兼容层”。
- 英文等价表达：`always`、`never`、`by default`、`across the project`、`going forward`、`for every`。
- 用户对某一类别给出稳定取舍，例如“管理后台以信息密度优先，采用紧凑布局”。

下面这些表达即使没有“以后”或“记住”，也属于明确长期：

- “所有业务 ID 用 string，API 边界保持类型一致。”
- “子智能体用于独立并行任务，顺序步骤由主智能体完成。”
- “危险操作必须二次确认，主按钮用图标加文字。”

用户一次说出多项内容时，逐项分类：明确长期的部分写入 Harness，任务细节继续留在当前任务上下文中。

## 抽象为可复用规范

先理解用户关注的问题、稳定偏好、适用对象和决策标准，再把它提炼成能指导未来同类任务的规范。最终条目采用项目指令形式，而非用户原话或对话摘要。

采用“抽象一层”的原则：

1. 去掉当前文件名、页面名、报错细节和对话语气等偶然信息。
2. 保留用户明确限定的领域、对象和硬约束，使规则范围与原意一致。
3. 从具体做法中提取稳定目的或选择标准，并以用户已经表达的信息为依据。
4. 让规则既能迁移到下一个同类任务，又能让执行者判断何时适用、应该怎么做。
5. 数值、类型、协议、强制项等精确约束具有执行价值时原样保留。

推荐把条目组织为“适用场景 + 原则/目标 + 必要约束”：

```markdown
- 管理后台界面优先支持高频扫描和重复操作：采用紧凑的信息密度、小型标题和紧凑的表格操作。
- 公共 API 演进优先保护现有调用方：保留兼容层，并在明确的弃用周期结束后移除旧调用方式。
- 可独立并行且上下文边界清晰的工作才拆分子智能体；简单顺序步骤由主智能体直接完成。
```

抽象示例：

- 原话：“这个订单页别放那么大的标题，表格也别一行一个卡片。”
  - 若用户语义指向管理后台同类页面，记录：“管理后台优先采用紧凑、可扫描的信息布局，使用与操作界面匹配的标题尺度和表格结构。”
- 原话：“迁移这个接口的时候旧参数先别删，调用方还没全改完。”
  - 若用户明确这是迁移的一贯要求，记录：“公共接口迁移需保留旧参数的兼容期，待调用方完成迁移后再按弃用流程移除。”
- 原话：“UserId 就用 string，别转 number。”
  - 记录：“业务标识使用 string，并在系统边界保持类型一致。”

用以下标准校准抽象层级：

- **抽象到位**：规则能指导下一个同类任务，而非复述当前对话。
- **范围准确**：规则覆盖用户表达的同类场景，并保持原有影响范围。

当抽象后的规则可能明显扩大影响面时，先确认抽象范围；如果只是去掉具体案例并保持同类场景，可直接记录。

## 按意图分流

Harness 仅收录用户表达的、非敏感的项目级长期规则。其余内容按自身用途处理：

- 一次性要求、当前文件或当前页面的选择应用于当前任务。
- 排错命令、实验步骤、待办、进度和实现状态保留在当前任务上下文中。
- 代码、配置和依赖事实继续以项目文件作为信息源。
- 单次方案选择保持为局部决策；用户将其概括为同类规则时再写入 Harness。
- 密钥、令牌和个人身份信息使用相应的安全存储机制。
- 跨项目个人偏好使用合适的全局偏好机制。

语义范围与跨任务复用性是主要判断依据。“必须在今天修好”属于当前交付；“这个页面统一成两列”属于单页选择；“所有管理页统一使用紧凑双栏”属于项目级长期规则。

长期规范以用户表达的规则为依据，保存动作由 skill 自动完成。

## 定位目标项目

按以下优先级确定项目根目录：

1. 用户明确指定的项目或仓库路径。
2. 当前任务实际修改文件所属的 Git 仓库根目录。
3. 当前工作区根目录；只有在它明确就是目标项目时才使用当前目录。

若当前会话涉及多个项目，把规范写入它实际约束的项目。项目归属不清时先确认目标项目。

在目标根目录查找 `AGENTS.md`。如果存在仅大小写不同的同名文件，沿用现有文件；否则创建规范名称 `AGENTS.md`。

## 处理规范冲突

写入前读取现有 `## Harness`，逐条比较新规范的适用范围、触发条件和要求动作。

把以下情况视为冲突：新旧规则作用于相同或重叠场景，并要求无法同时完成的做法。例如现有规则要求“业务 ID 始终使用 string”，新偏好要求“订单 API 的业务 ID 使用 number”。

语义细化、补充理由或提高明确程度，且两条规则可以同时成立时，按兼容更新处理。例如“扩展优先使用 ExtensionHost”细化为“扩展统一通过 ExtensionHost 注册”属于同一方向的强化。

发现冲突时执行以下流程：

1. 立即暂停 Harness 写入和用户原本的工作。
2. 向用户简洁列出：
   - 已记录规范及其适用范围。
   - 新识别规范及其适用范围。
   - 两者无法同时成立的具体位置。
3. 询问用户选择保留现有规范、采用新规范，还是为两者划分明确的适用范围。
4. 保持项目文件原样并等待用户回答。
5. 获得选择后更新 Harness，使最终规范唯一且一致，然后继续原本的工作。

冲突确认是阻塞步骤。其他长期规范仍按正常流程记录。

## 更新 Harness 章节

只编辑目标文件中标题为 `## Harness` 的二级章节：

1. 读取完整 `AGENTS.md`，理解现有层级和约束。
2. 若 `## Harness` 不存在，在文件末尾添加该章节；保持文件原有换行风格，并与前文留一个空行。
3. 按“抽象一层”原则把每项偏好改写成独立、简洁、可执行的项目指令。保留用户意图、适用范围和必要理由。
4. 将语义相同的内容合并到现有条目中，使每项规则保持唯一。
5. 新规范与旧规范冲突时，执行“处理规范冲突”流程，并依据用户确认结果更新条目。
6. 相关规则较多时在 `## Harness` 下使用简短的三级标题分组，例如 `### UI`、`### Compatibility`、`### Data`、`### Agent implementation`；单条规则直接放在 Harness 下。
7. 保持 `AGENTS.md` 其他章节、注释、顺序和格式原样。

推荐条目形式：

```markdown
## Harness

- 新增或迁移公共 API 时保留旧调用方式，并提供明确的弃用周期。
- 业务标识统一使用字符串类型，并在边界层保持类型一致。
- 涉及独立且可并行的调研任务时使用子智能体；简单顺序任务由主智能体直接完成。
```

规则应描述“以后怎么做”。例如将具体反馈提炼为“表单提交按钮使用图标加文字”。

## 执行与反馈

Harness 通常是其他任务的伴随动作：记录规范后继续完成用户原本的任务，让持久化动作保持轻量。

当检测到规范冲突时，以用户决策为继续工作的前置条件。

完成后简短说明：

- 写入或更新了哪条规范。
- 修改的 `AGENTS.md` 路径。
- 若合并、替换了旧规则，说明这一点。

若目标文件不可写或项目根目录无法确定，准确说明当前状态，并给出原本准备写入的规范文本。

## 自检

提交前检查：

- 这是用户明确表达的长期项目规范。
- 目标是正确项目根目录中的唯一 `AGENTS.md`。
- `## Harness` 只有一个，各条目语义唯一且彼此一致。
- 新旧规范的兼容性已经检查；检测到的冲突已经获得用户明确选择。
- 新条目是经过适度抽象的可复用规范。
- 新条目简短、可执行、仅包含非敏感信息，并保持用户原意的适用范围。
- 文件的其他章节保持原样。
